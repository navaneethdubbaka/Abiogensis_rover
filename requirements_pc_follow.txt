# PC GPU follow server (run alongside or instead of local YOLO on the Pi)
# pip install -r requirements_pc_follow.txt

fastapi>=0.109.0
uvicorn[standard]>=0.27.0
httpx>=0.27.0
python-dotenv>=1.0.0
ultralytics>=8.0.0
opencv-python-headless>=4.8.0
numpy>=1.24.0
